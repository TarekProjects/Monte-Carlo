// Tarek Frahi

#include <cmath>
using namespace std;

class Normal {

	public:
		
		Normal(void);
		~Normal(void);
		
		double N(double x);
		double uniformRandom();
		double normalRandom();
};